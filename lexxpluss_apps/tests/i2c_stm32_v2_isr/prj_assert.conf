# The same tests, with assertions compiled in. __ASSERT_NO_MSG is a compile-time decision, so the
# handler's response to an entry it cannot serve can only be checked by building it both ways.
CONFIG_ZTEST=y
CONFIG_I2C=y
CONFIG_ASSERT=y
CONFIG_TEST=y
CONFIG_ZTEST_ASSERT_HOOK=y
